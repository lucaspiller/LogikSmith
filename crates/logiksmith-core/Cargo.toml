[package]
name = "logiksmith-core"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[features]
default = ["timezones", "astronomy", "vendored-lua"]
timezones = ["jiff/tzdb-bundle-always"]
astronomy = []
http-inputs = []
webhook-inputs = []
external-inputs = ["http-inputs", "webhook-inputs"]
vendored-lua = ["mlua/vendored"]

[dependencies]
mlua = { version = "0.10", default-features = false, features = ["lua54"] }
jiff = { version = "0.2", default-features = false, features = ["std"] }
